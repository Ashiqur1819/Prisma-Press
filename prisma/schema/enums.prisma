enum ActiveStatus {
    ACTIVE
    INACTIVE
}

enum Role {
    USER
    AUTHOR
    ADMIN
}

enum PostStatus {
    PUBLISHED
    DRAFT
}

enum CommentStatus {
    APPROVED
    REJECT
}

enum SubscriptionStatus {
    APPROVED
    CANCELED
    EXPIRED
}